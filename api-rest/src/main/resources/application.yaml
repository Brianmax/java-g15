spring:
  application:
    name: api-rest
  datasource:
    username: postgres
    password: admin
    url: jdbc:postgresql://localhost:5432/spring
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
